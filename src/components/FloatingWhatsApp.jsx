function FloatingWhatsApp({ whatsappUrl }) {
  return (
    <a className="floating-whatsapp" href={whatsappUrl} aria-label="Escribir por WhatsApp">
      WhatsApp
    </a>
  );
}

export default FloatingWhatsApp;
